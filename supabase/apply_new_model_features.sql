-- =============================================================================
-- Новые функции на странице модели (dashboard/models/[id])
-- Выполнить в Supabase Dashboard → SQL Editor → New query → Run
-- =============================================================================
--
-- 1) ОСНОВНАЯ ИНФОРМАЦИЯ — «Назначить ответственного»
--    В окне «Редактировать основную информацию» добавлено поле выбора одного
--    ответственного из списка. Значение хранится в crm_model_info.
--
-- 2) ДОСТУПЫ К САЙТАМ — три основных (Stripchat, Chaturbate, My.club) и «+ сайт»
--    Таблица crm_model_accesses уже поддерживает любые сайты (поле site — текст).
--    Три основных сайтов и кнопка «+ сайт» реализованы в приложении;
--    изменений в базе данных для этого не требуется.
--
-- =============================================================================

-- Добавить колонку «ответственный за модель» (id оператора из списка ответственных)
ALTER TABLE crm_model_info
  ADD COLUMN IF NOT EXISTS responsible_operator_id text;
